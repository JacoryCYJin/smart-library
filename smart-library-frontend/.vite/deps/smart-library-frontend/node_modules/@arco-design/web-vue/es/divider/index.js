import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.js";
import _Divider from "@arco-design/web-vue/es/divider/divider.js";
const Divider = Object.assign(_Divider, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _Divider.name, _Divider);
  }
});
export { Divider as default };
