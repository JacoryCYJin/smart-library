import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.jstils/global-config.js";
import _Calendar from "@arco-design/web-vue/es/calendar/calendar.jsweb-vue/es/calendar/calendar.js";
const Calendar = Object.assign(_Calendar, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _Calendar.name, _Calendar);
  }
});
export { Calendar as default };
