import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.jstils/global-config.js";
import _VerificationCode from "@arco-design/web-vue/es/verification-code/verification-code.jss/verification-code/verification-code.js";
const VerificationCode = Object.assign(_VerificationCode, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _VerificationCode.name, _VerificationCode);
  }
});
export { VerificationCode as default };
