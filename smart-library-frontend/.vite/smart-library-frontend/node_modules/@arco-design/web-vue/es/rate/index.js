import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.jstils/global-config.js";
import _Rate from "@arco-design/web-vue/es/rate/rate.jsign/web-vue/es/rate/rate.js";
const Rate = Object.assign(_Rate, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _Rate.name, _Rate);
  }
});
export { Rate as default };
