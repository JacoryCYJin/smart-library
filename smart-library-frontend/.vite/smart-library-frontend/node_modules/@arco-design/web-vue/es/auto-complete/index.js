import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.jstils/global-config.js";
import _AutoComplete from "@arco-design/web-vue/es/auto-complete/auto-complete.jsue/es/auto-complete/auto-complete.js";
const AutoComplete = Object.assign(_AutoComplete, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _AutoComplete.name, _AutoComplete);
  }
});
export { AutoComplete as default };
