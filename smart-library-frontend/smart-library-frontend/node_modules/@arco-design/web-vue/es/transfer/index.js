import { setGlobalConfig, getComponentPrefix } from "@arco-design/web-vue/es/_utils/global-config.js";
import _Transfer from "@arco-design/web-vue/es/transfer/transfer.js";
const Transfer = Object.assign(_Transfer, {
  install: (app, options) => {
    setGlobalConfig(app, options);
    const componentPrefix = getComponentPrefix(options);
    app.component(componentPrefix + _Transfer.name, _Transfer);
  }
});
export { Transfer as default };
