<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.jacorycyjin.smartlibrary.backend.mapper.ResourceCategoryRelMapper">

    <!-- 根据资源ID列表查询分类ID列表（去重） -->
    <select id="selectCategoryIdsByResourceIds" resultType="java.lang.String">
        SELECT DISTINCT category_id
        FROM resource_category_rel
        WHERE resource_id IN
        <foreach collection="resourceIds" item="resourceId" open="(" separator="," close=")">
            #{resourceId}
        </foreach>
        AND deleted = 0
    </select>

</mapper>
